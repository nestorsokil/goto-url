apiVersion: v1
kind: Pod
metadata:
  name: "{{ include "gotourl.fullname" . }}-test-connection"
  labels:
{{ include "gotourl.labels" . | indent 4 }}
  annotations:
    "helm.sh/hook": test-success
spec:
  containers:
    - name: wget
      image: busybox
      command: ['wget']
      args:  ['{{ include "gotourl.fullname" . }}:{{ .Values.service.port }}']
  restartPolicy: Never
